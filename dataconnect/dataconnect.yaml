specVersion: "v1beta"
serviceId: "task-effort"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "task-effort-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
